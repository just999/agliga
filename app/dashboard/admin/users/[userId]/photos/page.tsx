import CardInnerWrapper from '@/components/card-inner-wrapper';

const UserPhotosPage = () => {
  return <CardInnerWrapper header='photo' body={<div>user photo pages</div>} />;
};

export default UserPhotosPage;
